import { RecipeSuggestions } from '@fridgeezy/types';

/**
 * RecipeSuggestion domain type
 *
 * Represents a user-suggested recipe that can be promoted to a full recipe.
 * This is a type alias for the database table type with singular naming.
 */
export type RecipeSuggestion = RecipeSuggestions;
